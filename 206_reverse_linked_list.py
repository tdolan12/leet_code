def reverseList(head):
bool_flag = True
new_list = []
while bool_flag = True:
    if head.value != none:
       new_list.insert(0,head.value()) 
    #new_list.insert(0,head.value())
    head.next()
    if head.value() == None:
        bool_flag = False
return new_list





print(reverseList(head))